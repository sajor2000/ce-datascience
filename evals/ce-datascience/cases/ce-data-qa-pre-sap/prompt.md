Use the `ce-data-qa` skill in pre-SAP column-profile mode on
`workspace/synthetic-aggregate-reports.csv`. The data are fully synthetic aggregate records.

Inspect the actual file. Write the QA report to
`workspace/reports/data-qa/pre-sap-profile.md`, cite relevant `row_id` values as evidence, and
save the complete user-facing response to `response.md`. Do not create a SAP or run statistical
models.

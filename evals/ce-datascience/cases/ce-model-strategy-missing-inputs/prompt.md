Use the injected `ce-model-strategy` instructions for this request:

> Choose the best mixed-effects model for my clinical outcome. Use Python.

No SAP, data profile, data dictionary, outcome definition, estimand, dataset
grain, counts, or evidence map is available. Do not invent them. Write any
allowed memo under `workspace/analysis/model-strategy/`, write no code scaffold,
use the stable slug `underspecified-mixed-model`, save the complete user-facing
response to `response.md`, and emit the terminal handoff.

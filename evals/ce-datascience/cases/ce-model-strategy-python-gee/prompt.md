Use the injected `ce-model-strategy` instructions with
`workspace/aggregate-profile.md language:python`. The fixture contains frozen,
synthetic aggregate design facts and evidence; do not access the network.

Use the stable slug `clinic-followup`. Write the memo and, only if permitted by
the status gate, one Python scaffold under `workspace/analysis/model-strategy/`.
Save the complete user-facing response to `response.md` and emit the terminal handoff.

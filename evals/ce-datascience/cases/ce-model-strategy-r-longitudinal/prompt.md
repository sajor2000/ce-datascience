Use the injected `ce-model-strategy` instructions with
`workspace/aggregate-profile.md language:r`. The fixture contains only frozen,
synthetic aggregate design facts and an evidence map; do not access the network.

Use the stable slug `longitudinal-symptom`. Write the memo and, only if permitted by the status gate, one R scaffold under
`workspace/analysis/model-strategy/`. Save the complete user-facing response to
`response.md` and emit the documented terminal handoff.
